<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyShop.Client.Views.Customer.CustomerDashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:buttons="using:MyShop.Client.Views.Components.Buttons"
    xmlns:cards="using:MyShop.Client.Views.Components.Cards"
    xmlns:layout="using:MyShop.Client.Views.Components.Layout"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:MyShop.Client.ViewModels.Customer"
    xmlns:components="using:MyShop.Client.Views.Components"
    xmlns:converters="using:MyShop.Client.Common.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource PageBackgroundBrush}">

    <Page.Transitions>
        <TransitionCollection>
            <EntranceThemeTransition FromVerticalOffset="50"/>
        </TransitionCollection>
    </Page.Transitions>

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:CurrencyConverter x:Key="CurrencyConverter"/>
    </Page.Resources>

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="ResponsiveStates">

                <!-- Large Desktop: 1920px+ -->
                <VisualState x:Name="LargeDesktopState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1920"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainContent.Padding"
                                Value="40"/>
                    </VisualState.Setters>
                </VisualState>

                <!-- Desktop: 1280-1919px -->
                <VisualState x:Name="DesktopState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1280"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainContent.Padding"
                                Value="32"/>
                    </VisualState.Setters>
                </VisualState>

                <!-- Tablet: 960-1279px -->
                <VisualState x:Name="TabletState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="960"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainContent.Padding"
                                Value="24"/>
                    </VisualState.Setters>
                </VisualState>

                <!-- Tablet Portrait: 640-959px -->
                <VisualState x:Name="TabletPortraitState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="640"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainContent.Padding"
                                Value="20"/>
                    </VisualState.Setters>
                </VisualState>

                <!-- Mobile: 0-639px (Default) -->
                <VisualState x:Name="MobileState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainContent.Padding"
                                Value="16"/>
                    </VisualState.Setters>
                </VisualState>

            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid x:Name="MainContent"
                  Padding="24"
                  RowSpacing="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0"
                            Spacing="16">

                    <!-- Page Header -->
                    <layout:PageHeader
                        Title="Customer Dashboard"
                        Subtitle="Welcome back! Here's your account overview"/>

                    <!-- Become Admin Banner (shows when no admin exists) -->
                    <components:BecomeAdminBanner
                        x:Name="BecomeAdminBannerControl"
                        Visibility="{x:Bind ViewModel.ShowBecomeAdminBanner, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                        ActivationSuccessful="OnActivationSuccessful"/>

                    <!-- Email Verification Warning -->
                    <InfoBar
                        Severity="Warning"
                        Title="Email Verification Required"
                        Message="Please verify your email to access all features"
                        IsOpen="{x:Bind ViewModel.IsVerified, Mode=OneWay, Converter={StaticResource InverseBoolConverter}}"
                        IsClosable="False">
                        <InfoBar.ActionButton>
                            <Button
                                Content="Resend Email"
                                MinWidth="140"
                                MaxWidth="150"
                                Click="ResendVerificationEmail_Click"
                                Style="{StaticResource AccentButtonStyle}"/>
                        </InfoBar.ActionButton>
                    </InfoBar>

                    <!-- Welcome Section -->
                    <Border Style="{StaticResource CardWithShadowStyle}"
                            Padding="24">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0"
                                        Spacing="8">
                                <TextBlock Text="{x:Bind ViewModel.WelcomeMessage, Mode=OneWay}"
                                           FontSize="{StaticResource FontSize_4XL}"
                                           FontWeight="Bold"
                                           TextWrapping="Wrap"
                                           Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                                <TextBlock Text="Discover amazing products and great deals"
                                           FontSize="{StaticResource FontSize_Base}"
                                           Foreground="{ThemeResource Gray400Brush}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1"
                                        Orientation="Horizontal"
                                        Spacing="12"
                                        VerticalAlignment="Center">
                                <Button Style="{StaticResource SecondaryButtonStyle}"
                                        Click="OnBecomeAgentClick"
                                        MinWidth="160"
                                        AutomationProperties.Name="Become sales agent"
                                        AutomationProperties.HelpText="Apply to become a sales agent"
                                        UseSystemFocusVisuals="True">
                                    <StackPanel Orientation="Horizontal"
                                                Spacing="8">
                                        <FontIcon Glyph="&#xE735;"
                                                  FontSize="{StaticResource FontSize_LG}"
                                                  Foreground="{ThemeResource AccentBlueBrush}"/>
                                        <TextBlock Text="Become Agent"
                                                   FontSize="{StaticResource FontSize_Base}"
                                                   FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Button>
                                <buttons:IconButton
                                    MinWidth="120"
                                    Variant="Filled"
                                    IconGlyph="&#xE719;"
                                    Text="Shop Now"
                                    Click="OnShopNowClick"
                                    AutomationProperties.Name="Shop now"
                                    AccessKey="N"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- KPI Stats Cards (P1 - Component Enforcement) -->
                    <Grid x:Name="KPIGrid"
                          ColumnSpacing="20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Total Orders KPI -->
                        <cards:KPICard
                            Grid.Column="0"
                            Title="Total Orders"
                            Value="{x:Bind ViewModel.TotalOrders, Mode=OneWay}"
                            Subtitle="All time"
                            Icon="&#xF133;"
                            IconBackgroundBrush="{ThemeResource PurpleLightBrush}"
                            IconForegroundBrush="{ThemeResource SecondaryBlueBrush}"/>

                        <!-- Total Spent KPI -->
                        <cards:KPICard
                            Grid.Column="1"
                            Title="Total Spent"
                            Value="{x:Bind ViewModel.TotalSpent, Mode=OneWay}"
                            Subtitle="All time"
                            Icon="&#xE8C7;"
                            IsCurrency="True"
                            IconBackgroundBrush="{ThemeResource WarningAmberLightBrush}"
                            IconForegroundBrush="{ThemeResource WarningAmberBrush}"/>

                        <!-- Pending Orders KPI -->
                        <cards:KPICard
                            Grid.Column="2"
                            Title="Pending Orders"
                            Value="{x:Bind ViewModel.PendingOrders, Mode=OneWay}"
                            Subtitle="Awaiting processing"
                            Icon="&#xE734;"
                            IconBackgroundBrush="{ThemeResource SuccessGreenLightBrush}"
                            IconForegroundBrush="{ThemeResource SuccessGreenBrush}"/>

                        <!-- Account Age KPI -->
                        <cards:KPICard
                            Grid.Column="3"
                            Title="Member Since"
                            Value="{x:Bind ViewModel.MemberSinceDays, Mode=OneWay}"
                            Subtitle="days ago"
                            Icon="&#xE77B;"
                            IconBackgroundBrush="{ThemeResource SubtleFillColorSecondaryBrush}"
                            IconForegroundBrush="{ThemeResource AccentBlueBrush}"/>
                    </Grid>

                </StackPanel>

                <!-- Main Content -->
                <StackPanel Grid.Row="1"
                            Spacing="20">

                    <!-- Featured Products Section -->
                    <StackPanel Spacing="16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="Featured Products"
                                       FontSize="{StaticResource FontSize_3XL}"
                                       FontWeight="Bold"
                                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                       AutomationProperties.HeadingLevel="Level2"/>

                            <HyperlinkButton Grid.Column="1"
                                             Content="View All →"
                                             Click="ViewAllFeaturedProducts_Click"
                                             FontSize="{StaticResource FontSize_Base}"
                                             Foreground="{ThemeResource AccentBlueBrush}"/>
                        </Grid>

                        <!-- Changed to {Binding} for async loaded API data -->
                        <ItemsRepeater ItemsSource="{Binding FeaturedProducts, Mode=OneWay}">
                            <ItemsRepeater.Layout>
                                <UniformGridLayout MinItemWidth="250"
                                                   MinItemHeight="320"
                                                   ItemsStretch="Fill"
                                                   MinRowSpacing="20"
                                                   MinColumnSpacing="20"/>
                            </ItemsRepeater.Layout>
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate x:DataType="viewmodels:FeaturedProductItem">
                                    <Border Style="{StaticResource ProductCardStyle}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="180"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <!-- Product Image -->
                                            <Border Grid.Row="0"
                                                    Background="{ThemeResource LayerFillColorDefaultBrush}"
                                                    CornerRadius="8"
                                                    Margin="0,0,0,12">
                                                <Image Source="{x:Bind Image}"
                                                       Stretch="UniformToFill"/>
                                            </Border>

                                            <!-- Product Info -->
                                            <StackPanel Grid.Row="1"
                                                        Spacing="8">
                                                <TextBlock Text="{x:Bind Category}"
                                                           FontSize="{StaticResource FontSize_SM}"
                                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                           TextTrimming="CharacterEllipsis"/>

                                                <TextBlock Text="{x:Bind Name}"
                                                           FontSize="{StaticResource FontSize_LG}"
                                                           FontWeight="SemiBold"
                                                           Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                                           TextWrapping="Wrap"
                                                           MaxLines="2"/>

                                                <!-- Price -->
                                                <TextBlock Text="{x:Bind Price, Converter={StaticResource CurrencyConverter}}"
                                                           FontSize="{StaticResource FontSize_2XL}"
                                                           FontWeight="Bold"
                                                           Foreground="{ThemeResource PrimaryBrush}"/>

                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </StackPanel>

                    <!-- Recommended Products Section -->
                    <StackPanel Spacing="16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="Recommended for You"
                                       FontSize="{StaticResource FontSize_3XL}"
                                       FontWeight="Bold"
                                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>

                            <HyperlinkButton Grid.Column="1"
                                             Content="View All →"
                                             Click="ViewAllRecommendedProducts_Click"
                                             FontSize="{StaticResource FontSize_Base}"
                                             Foreground="{ThemeResource AccentBlueBrush}"/>
                        </Grid>

                        <!-- Changed to {Binding} for async loaded API data -->
                        <ItemsRepeater ItemsSource="{Binding RecommendedProducts, Mode=OneWay}">
                            <ItemsRepeater.Layout>
                                <UniformGridLayout MinItemWidth="250"
                                                   MinItemHeight="320"
                                                   ItemsStretch="Fill"
                                                   MinRowSpacing="20"
                                                   MinColumnSpacing="20"/>
                            </ItemsRepeater.Layout>
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate x:DataType="viewmodels:RecommendedProductItem">
                                    <Border Style="{StaticResource ProductCardStyle}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="180"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <!-- Product Image -->
                                            <Border Grid.Row="0"
                                                    Background="{ThemeResource LayerFillColorDefaultBrush}"
                                                    CornerRadius="8"
                                                    Margin="0,0,0,12">
                                                <Image Source="{x:Bind Image}"
                                                       Stretch="UniformToFill"/>
                                            </Border>

                                            <!-- Product Info -->
                                            <StackPanel Grid.Row="1"
                                                        Spacing="8">
                                                <TextBlock Text="{x:Bind Category}"
                                                           FontSize="{StaticResource FontSize_SM}"
                                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                           TextTrimming="CharacterEllipsis"/>

                                                <TextBlock Text="{x:Bind Name}"
                                                           FontSize="{StaticResource FontSize_LG}"
                                                           FontWeight="SemiBold"
                                                           Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                                           TextWrapping="Wrap"
                                                           MaxLines="2"/>

                                                <!-- Price -->
                                                <TextBlock Text="{x:Bind Price, Converter={StaticResource CurrencyConverter}}"
                                                           FontSize="{StaticResource FontSize_2XL}"
                                                           FontWeight="Bold"
                                                           Foreground="{ThemeResource PrimaryBrush}"/>

                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </StackPanel>

                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
