<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyShop.Client.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
        <Grid>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="400" MaxWidth="400">
                <!-- Header -->
                <TextBlock Text="Đăng Nhập MyShop 2025" 
                          Style="{StaticResource TitleTextBlockStyle}" 
                          Margin="0,0,0,40" 
                          HorizontalAlignment="Center"
                          FontWeight="Bold"/>

                <!-- Login Card -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="32">
                    
                    <StackPanel Spacing="16">
                        <!-- Email Input -->
                        <StackPanel>
                            <TextBlock Text="Email / Tên đăng nhập" 
                                      Margin="0,0,0,6" 
                                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBox x:Name="UsernameTextBox"
                                    Text="{x:Bind ViewModel.Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    PlaceholderText="Nhập email hoặc tên đăng nhập"
                                    InputScope="Default"
                                    KeyDown="Input_KeyDown"
                                    IsEnabled="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"
                                    Background="{ThemeResource TextControlBackground}"
                                    BorderThickness="1"
                                    CornerRadius="4"/>
                            <!-- Username Validation Error -->
                            <TextBlock Text="{x:Bind ViewModel.UsernameError, Mode=OneWay}"
                                      Foreground="{ThemeResource SystemErrorTextColor}"
                                      FontSize="12"
                                      Margin="0,2,0,0"
                                      Visibility="{x:Bind ViewModel.UsernameError, Mode=OneWay, Converter={StaticResource StringToVisibilityConverter}}"/>
                        </StackPanel>

                        <!-- Password Input -->
                        <StackPanel>
                            <TextBlock Text="Mật khẩu" 
                                      Margin="0,0,0,6" 
                                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <PasswordBox x:Name="PasswordInput" 
                                        PlaceholderText="Nhập mật khẩu"
                                        PasswordChanged="PasswordInput_PasswordChanged"
                                        KeyDown="Input_KeyDown"
                                        IsEnabled="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"
                                        Background="{ThemeResource TextControlBackground}"
                                        BorderThickness="1"
                                        CornerRadius="4"/>
                            <!-- Password Validation Error -->
                            <TextBlock Text="{x:Bind ViewModel.PasswordError, Mode=OneWay}"
                                      Foreground="{ThemeResource SystemErrorTextColor}"
                                      FontSize="12"
                                      Margin="0,2,0,0"
                                      Visibility="{x:Bind ViewModel.PasswordError, Mode=OneWay, Converter={StaticResource StringToVisibilityConverter}}"/>
                        </StackPanel>

                        <!-- Error Message -->
                        <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" 
                                  Foreground="{ThemeResource SystemErrorTextColor}" 
                                  Visibility="{x:Bind ViewModel.ErrorMessage, Mode=OneWay, Converter={StaticResource StringToVisibilityConverter}}"
                                  TextWrapping="Wrap"
                                  HorizontalAlignment="Center"
                                  TextAlignment="Center"/>

                        <!-- Login Button -->
                        <Button HorizontalAlignment="Stretch"
                                Command="{x:Bind ViewModel.AttemptLoginCommand}"
                                Style="{StaticResource AccentButtonStyle}"
                                Margin="0,8,0,0">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" 
                                                 Width="16" Height="16"
                                                 Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    <TextBlock Text="ĐĂNG NHẬP"/>
                                </StackPanel>
                            </Button.Content>
                        </Button>

                        <!-- Navigate to Register -->
                        <HyperlinkButton Content="Chưa có tài khoản? Đăng ký ngay" 
                                        HorizontalAlignment="Center"
                                        Command="{x:Bind ViewModel.NavigateToRegisterCommand}"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Loading Overlay -->
            <Grid Background="#80000000" 
                  Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
                    <ProgressRing IsActive="True" Width="50" Height="50"/>
                    <TextBlock Text="Đang đăng nhập..." 
                              Foreground="White" 
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>