<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyShop.Client.Views.Errors.NoConnectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--
    No Connection Page
    ==================
    Displayed when there's no network/server connection.
    -->

    <Grid Background="{ThemeResource BackgroundLightBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition FromVerticalOffset="50"/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <StackPanel 
            HorizontalAlignment="Center" 
            VerticalAlignment="Center" 
            Spacing="24"
            MaxWidth="500"
            Padding="32">
            
            <!-- No Connection Illustration -->
            <Grid Width="200" Height="200" HorizontalAlignment="Center">
                <!-- Circle Background -->
                <Ellipse 
                    Width="200" Height="200" 
                    Fill="{ThemeResource WarningAmber50Brush}" 
                    Opacity="0.5"/>
                
                <!-- WiFi Off Icon -->
                <FontIcon 
                    Glyph="&#xEB5E;"
                    FontSize="80"
                    Foreground="{ThemeResource WarningAmberBrush}"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"/>
            </Grid>
            
            <!-- Title -->
            <TextBlock 
                Text="No Connection" 
                FontSize="28" 
                FontWeight="Bold" 
                Foreground="{ThemeResource TextPrimaryBrush}"
                HorizontalAlignment="Center"/>
            
            <!-- Description -->
            <TextBlock 
                Text="Unable to connect to the server. Please check your internet connection and try again."
                FontSize="15" 
                Foreground="{ThemeResource TextMutedBrush}"
                TextAlignment="Center"
                TextWrapping="Wrap"/>
            
            <!-- Tips Box -->
            <Border 
                Background="{ThemeResource Gray100Brush}"
                CornerRadius="8"
                Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock 
                        Text="Troubleshooting Tips:"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Foreground="{ThemeResource TextPrimaryBrush}"/>
                    
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE73E;" FontSize="12" Foreground="{ThemeResource SuccessGreenBrush}"/>
                            <TextBlock Text="Check your Wi-Fi or Ethernet connection" FontSize="13" Foreground="{ThemeResource TextMutedBrush}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE73E;" FontSize="12" Foreground="{ThemeResource SuccessGreenBrush}"/>
                            <TextBlock Text="Verify the server address in settings" FontSize="13" Foreground="{ThemeResource TextMutedBrush}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE73E;" FontSize="12" Foreground="{ThemeResource SuccessGreenBrush}"/>
                            <TextBlock Text="Make sure the server is running" FontSize="13" Foreground="{ThemeResource TextMutedBrush}"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- Action Buttons -->
            <StackPanel 
                Orientation="Horizontal" 
                HorizontalAlignment="Center" 
                Spacing="12">
                
                <!-- Retry Button -->
                <Button 
                    x:Name="RetryButton"
                    Padding="24,12"
                    Background="{ThemeResource PrimaryBrush}"
                    Foreground="White"
                    BorderThickness="0"
                    CornerRadius="8"
                    Click="RetryButton_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon x:Name="RetryIcon" Glyph="&#xE72C;" FontSize="14"/>
                        <ProgressRing 
                            x:Name="RetryProgress" 
                            Width="14" Height="14" 
                            Foreground="White"
                            Visibility="Collapsed"/>
                        <TextBlock x:Name="RetryText" Text="Retry Connection" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
                
                <!-- Configure Server Button -->
                <Button 
                    x:Name="ConfigureButton"
                    Padding="24,12"
                    Background="Transparent"
                    BorderBrush="{ThemeResource BorderDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Click="ConfigureButton_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE713;" FontSize="14"/>
                        <TextBlock Text="Settings"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
