<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="MyShop.Client.Views.Admin.AdminUsersPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:badges="using:MyShop.Client.Views.Components.Badges"
    xmlns:buttons="using:MyShop.Client.Views.Components.Buttons"
    xmlns:components="using:MyShop.Client.Views.Components"
    xmlns:forms="using:MyShop.Client.Views.Components.Forms"
    xmlns:converters="using:MyShop.Client.Common.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:skeletons="using:MyShop.Client.Views.Components.Skeletons"
    xmlns:viewmodels="using:MyShop.Client.ViewModels.Admin"
    xmlns:layout="using:MyShop.Client.Views.Components.Layout"
    xmlns:tables="using:MyShop.Client.Views.Components.Tables"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Page.Transitions>
        <TransitionCollection>
            <EntranceThemeTransition FromVerticalOffset="50"/>
        </TransitionCollection>
    </Page.Transitions>

    <Page.Resources>
        <converters:StatusToVariantConverter x:Key="StatusToVariantConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:CountToVisibilityConverter x:Key="CountToVisibility"/>
        <converters:CountToVisibilityConverter x:Key="InvertCountToVisibility"
                                               IsInverted="True"/>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <converters:BoolToStringConverter x:Key="BoolToStringConverter"/>
        <converters:BoolToGlyphConverter x:Key="BoolToGlyphConverter"/>
        <converters:StringToImageSourceConverter x:Key="StringToImageSourceConverter"/>
    </Page.Resources>

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="ResponsiveStates">

                <!-- Large Desktop: 1920px+ -->
                <VisualState x:Name="LargeDesktopState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1920"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainContent.Padding"
                                Value="40"/>
                    </VisualState.Setters>
                </VisualState>

                <!-- Desktop: 1280-1919px -->
                <VisualState x:Name="DesktopState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1280"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainContent.Padding"
                                Value="32"/>
                    </VisualState.Setters>
                </VisualState>

                <!-- Tablet: 960-1279px -->
                <VisualState x:Name="TabletState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="960"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainContent.Padding"
                                Value="24"/>
                    </VisualState.Setters>
                </VisualState>

                <!-- Tablet Portrait: 640-959px -->
                <VisualState x:Name="TabletPortraitState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="640"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainContent.Padding"
                                Value="20"/>
                    </VisualState.Setters>
                </VisualState>

                <!-- Mobile: 0-639px (Default) -->
                <VisualState x:Name="MobileState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="MainContent.Padding"
                                Value="16"/>
                    </VisualState.Setters>
                </VisualState>

            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <RefreshContainer RefreshRequested="RefreshContainer_RefreshRequested">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid x:Name="MainContent"
                        Padding="24"
                      RowSpacing="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!--  Header  -->
                    <layout:PageHeader
                        Grid.Row="0"
                        Title="Users Management"
                        Subtitle="Manage all users in the system">
                        <layout:PageHeader.ActionContent>
                            <buttons:IconButton
                                MinWidth="140"
                                Variant="Filled"
                                IconGlyph="&#xE710;"
                                Text="Add New User"
                                Command="{x:Bind ViewModel.AddNewUserCommand}"
                                Tooltip="Create a new user account"
                                AutomationProperties.Name="Add new user, press Alt+N"
                                AccessKey="N"/>
                        </layout:PageHeader.ActionContent>
                    </layout:PageHeader>


                    <!-- Main Content -->
                    <StackPanel Grid.Row="1"
                                Spacing="20">

                        <!--  Search Card  -->
                        <Border Style="{StaticResource CardWithShadowStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Spacing="4">
                                    <TextBlock
                                        FontSize="14"
                                        FontWeight="SemiBold"
                                        Foreground="{ThemeResource TextTertiaryBrush}"
                                        Text="Search Users"
                                        AutomationProperties.HeadingLevel="Level2"/>
                                    <TextBlock
                                        FontSize="12"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="Find users by name, email, or phone number"/>
                                </StackPanel>

                                <forms:SearchCard
                                    x:Name="UserSearchCard"
                                    Grid.Column="1"
                                    Width="350"
                                    HorizontalAlignment="Right"
                                    PlaceholderText="Search users..."
                                    SearchText="{x:Bind ViewModel.PendingSearchQuery, Mode=TwoWay}"
                                    ShowSearchButton="Collapsed"
                                    TextChanged="SearchCard_TextChanged"
                                    SuggestionChosen="SearchCard_SuggestionChosen"
                                    QuerySubmitted="SearchCard_QuerySubmitted"/>
                            </Grid>
                        </Border>

                        <!--  Filters Card  -->
                        <Border Style="{StaticResource CardWithShadowStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!--  Role Filter  -->
                                <StackPanel Spacing="4">
                                    <TextBlock
                                        FontSize="12"
                                        FontWeight="SemiBold"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="Role"/>
                                    <ComboBox
                                        x:Name="RoleComboBox"
                                        Width="150"
                                        Padding="12"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        PlaceholderText="All Roles"
                                        AutomationProperties.Name="Role filter"
                                        AutomationProperties.HelpText="Filter users by role"
                                        UseSystemFocusVisuals="True"
                                        SelectionChanged="RoleComboBox_SelectionChanged">
                                        <ComboBoxItem Content="All Roles"
                                                      Tag="All Roles"
                                                      IsSelected="True"/>
                                        <ComboBoxItem Content="Admin"
                                                      Tag="Admin"/>
                                        <ComboBoxItem Content="Sales Agent"
                                                      Tag="SalesAgent"/>
                                        <ComboBoxItem Content="Customer"
                                                      Tag="Customer"/>
                                    </ComboBox>
                                </StackPanel>

                                <!--  Apply Filters Button  -->
                                <buttons:IconButton
                                    Grid.Column="3"
                                    VerticalAlignment="Bottom"
                                    Variant="Filled"
                                    IconGlyph="&#xE71C;"
                                    Text="Apply"
                                    Command="{x:Bind ViewModel.ApplyFiltersCommand}"
                                    ToolTipService.ToolTip="Apply filters"/>

                                <!--  Reset Filters Button  -->
                                <buttons:IconButton
                                    Grid.Column="4"
                                    Margin="8,0,0,0"
                                    VerticalAlignment="Bottom"
                                    Variant="Outline"
                                    IconGlyph="&#xE72C;"
                                    Text="Reset"
                                    Command="{x:Bind ViewModel.ResetFiltersCommand}"
                                    ToolTipService.ToolTip="Reset all filters"/>

                                <!--  Export Button  -->
                                <buttons:IconButton
                                    Grid.Column="5"
                                    Margin="8,0,0,0"
                                    VerticalAlignment="Bottom"
                                    Variant="Outline"
                                    IconGlyph="&#xE898;"
                                    Text="Export"
                                    Click="ExportButton_Click"
                                    ToolTipService.ToolTip="Export users to CSV"/>
                            </Grid>
                        </Border>

                        <!--  Users Table  -->
                        <Border Style="{StaticResource CardWithShadowStyle}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!--  Loading Skeleton  -->
                                <skeletons:UserListSkeleton
                                    Grid.Row="0"
                                    Grid.RowSpan="2"
                                    Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>

                                <!--  Empty State - shown when no users  -->
                                <StackPanel
                                    Grid.Row="0"
                                    Grid.RowSpan="2"
                                    Padding="80,60"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Spacing="20"
                                    Visibility="Collapsed">
                                    <StackPanel.ChildrenTransitions>
                                        <TransitionCollection>
                                            <EntranceThemeTransition FromVerticalOffset="50"/>
                                        </TransitionCollection>
                                    </StackPanel.ChildrenTransitions>

                                    <Viewbox Width="120"
                                             Height="120">
                                        <Grid>
                                            <Ellipse Width="120"
                                                     Height="120"
                                                     Fill="{ThemeResource SubtleFillColorSecondaryBrush}"/>
                                            <FontIcon
                                                FontSize="64"
                                                Foreground="{ThemeResource Gray500Brush}"
                                                Glyph="&#xE77B;"/>
                                        </Grid>
                                    </Viewbox>

                                    <TextBlock
                                        FontSize="20"
                                        FontWeight="SemiBold"
                                        Foreground="{ThemeResource TextTertiaryBrush}"
                                        Text="No users found"
                                        TextAlignment="Center"/>
                                    <TextBlock
                                        FontSize="14"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="Start by adding your first user to the system"
                                        TextAlignment="Center"
                                        MaxWidth="300"
                                        TextWrapping="Wrap"/>

                                    <Button
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Margin="0,12,0,0"
                                        ToolTipService.ToolTip="Create a new user account">
                                        <StackPanel Orientation="Horizontal"
                                                    Spacing="8">
                                            <FontIcon FontSize="16"
                                                      Glyph="&#xE710;"/>
                                            <TextBlock Text="Add First User"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>

                                <!--  Table Header  -->
                                <tables:TableHeader Grid.Row="0">
                                    <tables:TableHeader.Columns>
                                        <x:String>3*|User</x:String>
                                        <x:String>*|Role</x:String>
                                        <x:String>2*|Phone</x:String>
                                        <x:String>*|Status</x:String>
                                        <x:String>3*|Actions|Right</x:String>
                                    </tables:TableHeader.Columns>
                                </tables:TableHeader>

                                <!--  Users List  -->
                                <ItemsRepeater Grid.Row="1" ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}">
                                    <ItemsRepeater.ItemTemplate>
                                        <DataTemplate x:DataType="viewmodels:UserViewModel">
                                            <Grid
                                                Padding="16"
                                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                BorderThickness="0,1,0,0">
                                                <Grid.ChildrenTransitions>
                                                    <TransitionCollection>
                                                        <EntranceThemeTransition/>
                                                        <RepositionThemeTransition/>
                                                    </TransitionCollection>
                                                </Grid.ChildrenTransitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="3*"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="2*"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="3*"/>
                                                </Grid.ColumnDefinitions>

                                                <!--  User Info  -->
                                                <tables:UserAvatarInfo
                                                    Grid.Column="0"
                                                    DisplayName="{x:Bind Name, Mode=OneWay}"
                                                    SecondaryText="{x:Bind Email, Mode=OneWay}"
                                                    AvatarUrl="{x:Bind Avatar, Mode=OneWay}"/>

                                                <!--  Role Badge  -->
                                                <Border
                                                    Grid.Column="1"
                                                    Padding="8,4"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Center"
                                                    Background="{x:Bind RoleBgColor, Mode=OneWay}"
                                                    CornerRadius="6">
                                                    <TextBlock
                                                        FontSize="12"
                                                        FontWeight="SemiBold"
                                                        Foreground="{x:Bind RoleColor, Mode=OneWay}"
                                                        Text="{x:Bind Role, Mode=OneWay}"/>
                                                </Border>

                                                <!--  Phone  -->
                                                <TextBlock
                                                    Grid.Column="2"
                                                    VerticalAlignment="Center"
                                                    FontSize="14"
                                                    Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                                    Text="{x:Bind Phone, Mode=OneWay}"/>

                                                <!--  Status Badge  -->
                                                <badges:StatusBadge
                                                    Grid.Column="3"
                                                    Text="{x:Bind Status, Mode=OneWay}"
                                                    Variant="{x:Bind Status, Mode=OneWay, Converter={StaticResource StatusToVariantConverter}}"
                                                    ShowIcon="True"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Center"/>

                                                <!--  Actions  -->
                                                <StackPanel
                                                    Grid.Column="4"
                                                    HorizontalAlignment="Right"
                                                    Orientation="Horizontal"
                                                    Spacing="8">
                                                </StackPanel>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsRepeater.ItemTemplate>
                                </ItemsRepeater>

                            </Grid>
                        </Border>

                        <!--  Pagination (TASK-08 Standardized)  -->
                        <Border
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16,12"
                            Margin="0,16,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!--  Previous Button  -->
                                <Button
                                    Grid.Column="0"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Content="Previous"
                                    IsEnabled="{x:Bind ViewModel.HasPreviousPage, Mode=OneWay}"
                                    Command="{x:Bind ViewModel.PreviousPageCommand}"/>

                                <!--  Page Info  -->
                                <StackPanel
                                    Grid.Column="1"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Spacing="4">
                                    <TextBlock
                                        FontSize="14"
                                        FontWeight="SemiBold"
                                        Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                        HorizontalAlignment="Center"
                                        Text="{x:Bind ViewModel.PageInfoText, Mode=OneWay}"/>
                                    <TextBlock
                                        FontSize="12"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        HorizontalAlignment="Center"
                                        Text="{x:Bind ViewModel.ItemsInfoText, Mode=OneWay}"/>
                                </StackPanel>

                                <!--  Next Button  -->
                                <Button
                                    Grid.Column="2"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Content="Next"
                                    IsEnabled="{x:Bind ViewModel.HasNextPage, Mode=OneWay}"
                                    Command="{x:Bind ViewModel.NextPageCommand}"/>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </RefreshContainer>
    </Grid>
</Page>

