<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyShop.Client.Views.DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
            <TextBlock Text="{x:Bind ViewModel.WelcomeMessage, Mode=OneWay}" 
                      Style="{StaticResource HeaderTextBlockStyle}" 
                      HorizontalAlignment="Center"
                      TextAlignment="Center"/>
            
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="40">
                
                <StackPanel Spacing="16">
                    <FontIcon Glyph="&#xE10F;" FontSize="48" HorizontalAlignment="Center"/>
                    <TextBlock Text="Dashboard" 
                              Style="{StaticResource TitleTextBlockStyle}" 
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="Chức năng dashboard sẽ được phát triển ở giai đoạn tiếp theo." 
                              HorizontalAlignment="Center"
                              TextAlignment="Center"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>