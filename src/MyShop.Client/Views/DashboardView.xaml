<!-- Views/DashboardView.xaml -->
<Page
    x:Class="MyShop.Client.Views.DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
        <Grid Width="120" Height="120">
            <Ellipse Fill="{ThemeResource AccentFillColorDefaultBrush}"/>
            <TextBlock Text="{x:Bind ViewModel.UserInitial, Mode=OneWay}"
                       FontSize="60"
                       FontWeight="Bold"
                       Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Grid>

        <TextBlock Text="{x:Bind ViewModel.CurrentUser.Username, Mode=OneWay, FallbackValue='Username'}" 
                   FontSize="28" 
                   FontWeight="SemiBold"
                   HorizontalAlignment="Center"/>

        <TextBlock Text="{x:Bind ViewModel.CurrentUser.Email, Mode=OneWay, FallbackValue='email@example.com'}" 
                   FontSize="18" 
                   HorizontalAlignment="Center"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

        <Button Content="Logout" Command="{x:Bind ViewModel.LogoutCommand}" Margin="0,30,0,0"/>
    </StackPanel>
</Page>