<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MyShop.Client.Views.Dashboard.CustomerDashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:MyShop.Client.ViewModels.Dashboard"
    mc:Ignorable="d"
    Background="{ThemeResource LayerFillColorDefaultBrush}">

    <Grid Padding="32" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel Spacing="16" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Customer Dashboard" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="Welcome, " FontSize="16" HorizontalAlignment="Center">
                <Run Text="Welcome, "/>
                <Run Text="{x:Bind ViewModel.CurrentUser.Username, Mode=OneWay}"/>
            </TextBlock>
            <Button Content="Logout" Command="{x:Bind ViewModel.LogoutCommand}" Width="120"/>
        </StackPanel>
    </Grid>
</Page>
