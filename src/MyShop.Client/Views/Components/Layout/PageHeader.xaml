<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MyShop.Client.Views.Components.Layout.PageHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="RootControl">

    <!--
    PageHeader Component v2.0 - RELEASE-GRADE
    ==========================================
    Standardized page header with fixed height, title, breadcrumb, and action buttons.
    
    SPECIFICATIONS:
    • Height: 64px fixed
    • Left: Title (24px SemiBold) + Breadcrumb (optional)
    • Right: Action buttons (12px spacing enforced)
    • Bottom border: BorderDefaultBrush (1px separator)
    • Composition: Uses Button System (no custom button styles)
    
    USAGE:
    <layout:PageHeader 
        Title="Users Management"
        Breadcrumb="Home > Admin">
        <layout:PageHeader.ActionContent>
            <Button Content="Add User" Style="{StaticResource PrimaryButtonStyle}"/>
        </layout:PageHeader.ActionContent>
    </layout:PageHeader>
    -->

    <Grid Height="64"
          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
          BorderThickness="0,0,0,1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Title Section -->
        <StackPanel Grid.Column="0" 
                    VerticalAlignment="Center"
                    Spacing="4">
            <!-- Breadcrumb (Optional) -->
            <TextBlock Text="{Binding Breadcrumb, ElementName=RootControl, Mode=OneWay}"
                       FontSize="12"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Visibility="{Binding Breadcrumb, ElementName=RootControl, Mode=OneWay, Converter={StaticResource EmptyStringToVisibilityConverter}}"/>
            
            <!-- Title (24px SemiBold - Heading2Style) -->
            <TextBlock Text="{Binding Title, ElementName=RootControl, Mode=OneWay}"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                       AutomationProperties.HeadingLevel="Level1"/>
        </StackPanel>

        <!-- Actions Slot (12px spacing enforced) -->
        <StackPanel Grid.Column="1"
                    Orientation="Horizontal"
                    VerticalAlignment="Center"
                    Spacing="12">
            <ContentPresenter Content="{Binding ActionContent, ElementName=RootControl, Mode=OneWay}"/>
        </StackPanel>
    </Grid>
</UserControl>
